---
description: Git commit and push workflow
alwaysApply: true
---

# Git Push

This repo has a single structure. The `unit` folder is a normal directory (no submodule).

**When committing and pushing:**
- Work from repo root: `git add ... && git commit ... && git push`
- Main app code lives under `unit/`.
