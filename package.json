{
  "name": "unit-ssr",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --ssr src/entry.express.ts --outDir dist/server",
    "build.client": "vite build --ssr=false",
    "build.server": "vite build --ssr",
    "start": "node dist/server/entry.express.js"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.18.0",
    "@builder.io/qwik-city": "^1.18.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.4.0"
  }
}
