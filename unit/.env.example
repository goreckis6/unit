# Database (Prisma)
# Dev: relative path (resolves from app root when running npm run dev:next)
# Prod: set by deploy workflow to file:/var/www/db-calculino/dev.db
DATABASE_URL="file:./prisma/dev.db"

# Production override (deploy sets this automatically; for local prod testing):
# DATABASE_URL="file:/var/www/db-calculino/dev.db"
# PostgreSQL alternative:
# DATABASE_URL="postgresql://user:password@localhost:5432/calculinohub?schema=public"

# Admin session signing (use a long random string in production)
ADMIN_SESSION_SECRET=your-secure-random-secret-at-least-32-chars

# Ollama Cloud - for Generate blog post and Translate features
OLLAMA_API_KEY=your-ollama-cloud-api-key
# Optional: model name (default: glm-4.6:cloud)
# OLLAMA_MODEL=glm-4.6:cloud

# Anthropic Claude API - for Generate content (Claude 4.6)
ANTHROPIC_API_KEY=your-anthropic-api-key
# Optional: model (default: claude-opus-4-6)
# CLAUDE_MODEL=claude-opus-4-6
